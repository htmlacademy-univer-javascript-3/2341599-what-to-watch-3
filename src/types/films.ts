export type AddReviewFilmType = {
  backgroundImage: string;
  peviewImage: string;
  title: string;
};

export type SeeReviewFilmType = {
  id: string;
  date: string;
  user: string;
  comment: string;
  rating: number;
}
