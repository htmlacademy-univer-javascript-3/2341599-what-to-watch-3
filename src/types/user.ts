export type AuthData = {
  login: string;
  password: string;
};

export type UserData = {
  id: number;
  email: string;
  token: string;
};

export type UserInfo = {
  avatarUrl: string;
  email: string;
  name: string;
  token: string;
}
